# quickrobot Python dependencies
# Generated 2026-05-23 — Updated 2026-07-26

# ============================================================
# CORE — All available as Debian apt python3-* packages
# ============================================================
# See below for packages that are all in the Debian Trixie (13) repos.
# Use the apt install string below instead of pip3 (optionally) for deployments on Debian/Ubuntu/Mint. (to be tested fully)
# ============================================================

# Web framework + templates
Flask
Jinja2
MarkupSafe
#Flask==3.1.1
#Jinja2==3.1.6
#MarkupSafe==2.1.5

# Automation / playbooks
ansible
ansible-core
PyYAML
#ansible==12.0.0
#ansible-core>=2.19.11rc1
#PyYAML==6.0.2

# HTTP client + auth (MCP proxy, SSH negotiation)
requests
requests_ntlm
#requests==2.32.3
#requests_ntlm==1.3.0

# CORS + WebSocket support
flask-cors
Flask-SocketIO
#flask-cors==5.0.0
#Flask-SocketIO==5.5.1

# Process monitoring (conditionally imported)
psutil
#psutil==7.0.0

# WSGI production server (API — replaces Flask dev server)
waitress
#waitress==3.0.2

# ============================================================
# MCP — Standalone (installed via pipx, not pip3)
# ============================================================
# The MCP SDK lives in a separate pipx environment.
# This keeps it isolated from the main quickrobot Python env.
# ============================================================
# pipx install mcp 
# pipx install websocket-client

# ============================================================
# DEBIAN APT — Required packages (all python3-*)
# ============================================================
# sudo apt update && sudo apt install -y ansible-core python3-flask python3-jinja2 python3-markupsafe python3-yaml python3-psutil python3-requests python3-requests-ntlm python3-flask-cors python3-flask-socketio python3-waitress

# ============================================================
# DEBIAN APT — Suggested dev tools
# ============================================================
# Git, tmux, curl, jq — useful for debugging, health checks,
# and manual operations (not required for the API to function).
# sudo apt update && sudo apt install -y git tmux curl jq
