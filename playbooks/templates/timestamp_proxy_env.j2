# @playbook_id: TEMPLATE_TIMESTAMP_PROXY_ENV
# @version: 2
{% for k, v in (merged_env).items() %}{% if v is not none and v != '' %}{{ k }}="{{ v | replace('"', '\\"') }}"
{% endif %}{% endfor %}QR_CLI_ARGS_JOINED="{{ cli_args_joined | replace('"', '\\"') }}"
